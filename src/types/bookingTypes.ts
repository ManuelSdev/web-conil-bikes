export type BookingStatusLabel =
   | 'pendiente'
   | 'activa'
   | 'finalizada'
   | 'cancelada'

export enum BookingStatusEnum {
   PENDING = 'pending',
   ACTIVE = 'active',
   FINISHED = 'finished',
   CANCELLED = 'cancelled',
}
